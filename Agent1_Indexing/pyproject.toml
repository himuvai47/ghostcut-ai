[project]
name = "facelessrc-agent1-indexing"
version = "1.0.2"
description = "100% local shot/clip-centric semantic footage indexer for FacelessRC"
requires-python = ">=3.11"
dependencies = ["numpy>=2.2", "Pillow>=11.0"]

[tool.setuptools]
packages = ["indexing_agent"]
